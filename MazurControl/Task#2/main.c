#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n, n!!;//імёны зменных складаюцца з малых і вялікіх ангельскіх літар, сімвала падкрэслівання '_' і лікаў (яны не могут быць з пачатку)
    do
    {
        printf("Type 'n', more than 0: ");
        scanf("%d", &n);
    }
    while(n<=0);
    for(int i=2)
    n!!=n()




        return 0;
}
